"""PolyNEAT example scripts, importable as a package.

Examples run as modules from the repository root, e.g.
``uv run python -m examples.xor.neat [--cpu | --gpu]``. Every intra-example
import is an absolute package import (``from examples._datasets import ...``),
so no path manipulation is needed here.
"""
